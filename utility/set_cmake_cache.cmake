function(append_cache cache_var data)
    set(spaced_string " ${${cache_var}} ")
    string(FIND "${spaced_string}" " ${data} " data_index)
    if(NOT data_index EQUAL -1)
        return()
    endif()
    string(COMPARE EQUAL "" "${${cache_var}}" is_empty)
    if(is_empty)
        set("${cache_var}" "${data}" CACHE STRING "" FORCE)
    else()
        set("${cache_var}" "${data} ${${cache_var}}" CACHE STRING "" FORCE)
    endif()
endfunction()


function(set_cache cache_var type data message)
    set(spaced_string " ${${cache_var}} ")
    string(FIND "${spaced_string}" " ${data} " data_index)
    if(NOT data_index EQUAL -1)
        return()
    endif()
    set("${cache_var}" "${data} ${${cache_var}}" CACHE "${type}" "${message}" FORCE)
endfunction()
